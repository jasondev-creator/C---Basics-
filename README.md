# C Programming

##  Topics Learned

- Comparison Operators (`==`, `!=`, `>=`, `<=`)
- Comments
  - `//` Single-line comment
  - `/* ... */` Multi-line comment
- BODMAS in C
- `printf()` for output
- `scanf()` for user input

##  What I Understood

- `printf()` makes the computer talk.
- `scanf()` lets the computer receive input from the user and store it in a variable.
- In C:
  - `1` = True
  - `0` = False

## 😭 Mistakes I Made

- Used `/n` instead of `\n`
- Confused `char` with `char[]`
- Forgot the correct comment syntax

## ✅ Lesson

Mistakes help me understand programming better.
Never Zero. Never Overload.

